name: Trading Service CI

on:
  push:
    branches:
      - 'feature/**'
      - dev
      - qa
      - master

  pull_request:
    branches:
      - dev
      - qa
      - master

permissions:
  contents: write

jobs:
  trading-ci:
    name: Trading Pipeline

    uses: Thamizhan-M3/TradeFlow-CommonTemplate/.github/workflows/_ci_template.yaml@master

    with:
      working_directory: .
      image_name: tradeflow-trading
      service_name: tradeflow-trading
      run_build: false
      sonar_project_key: tradeflow-trading
      docker_context: .
      dockerfile_path: ./Dockerfile

    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}